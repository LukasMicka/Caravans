{% extends 'base_admin.html.twig' %}

{% block title %}Caravan{% endblock %}

{% block body %}
    <h1>Caravan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caravan.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ caravan.name }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ caravan.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('caravan_index') }}">back to list</a>

    <a href="{{ path('caravan_edit', {'id': caravan.id}) }}">edit</a>

    {{ include('caravan_admin/_delete_form.html.twig') }}
{% endblock %}
